server:
  port: 8090
spring:
  config:
    activate:
      on-profile: local
  application:
    name: hdi-rural-busca-bff-api

logging:
  file: hdi-rural-busca-bff.log
  org.hibernate: INFO

feign:
  client:
    config:
      default:
        connectTimeout: 50000
        readTimeout: 50000
        loggerLevel: basic